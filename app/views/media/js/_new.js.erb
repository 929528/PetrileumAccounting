if ($('#new-media-window').is(':hidden')) {
$('#new-media-window').html("<%=j render 'media/media', item: item %>").attr('timestamp', $.now()).mw('show');
} else {
	alert('Обработать уже открытую форму');
};
